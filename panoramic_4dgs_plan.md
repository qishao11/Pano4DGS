# 全景 4DGS 研发计划

**写于 2026-08-16，基于 §3.30 之后的状态。**
入口文档是 `PROJECT_STATUS.md`；实验流水账在 `panoramic_4dgs_status.md`；本文件只讲**接下来做什么、按什么顺序、拿什么判据收尾**。

---

## 0. 一句话判断

表示不再是瓶颈——手上已经有三个能用的候选（逐时刻 bank、物体级 SE(3)、原生 4D 切片）。**瓶颈是当前数据把它们全都压在噪声底下，区分不开**。所以先修测量能力，再谈选哪个表示。

---

## 1. 真正的阻塞，按证据排序

| # | 阻塞 | 证据 | 后果 |
|---|---|---|---|
| 1 | **质心定位噪声压过物体运动** | 对齐后残差 0.559 vs 每帧位移 0.263 = **2.12×**（§3.29 数据，§3.30 复查） | 信噪比 <0.5，任何基于位置的速度估计都不成立 |
| 2 | **合成物体动得太小** | 每帧位移 = 球直径的 22%，相邻帧重叠 78% | v=0 打平上界 v=gt（0.263 / 0.259），4D 的插值能力测不出来 |
| 3 | **动态 bank 离群污染** | 12 个里 6 个跨度 27~102 世界单位（房间才 ~0.5） | 中点误差 21~122；只有 2/11 行可用 |
| 4 | **所有结论共享同一个产物** | §3.29/§3.30/校验全部重放 `objid_smoke2` | 地基若有系统性问题，结论一起塌 |

1 和 2 是同一件事的两面：**要么让信号变大，要么让噪声变小**。

---

## 2. 阶段计划

### 阶段 0：恢复测量能力（最高优先）— ✅ 已完成 2026-08-16，见 §3.31

没有这一步，后面所有对比都是在噪声里挑数字。

- **做什么**：`tools/make_synthetic_erp_room.py` 的三种运动 profile 加一个幅度参数，生成每帧位移 ≳ 1 个球直径的序列（约现在的 5 倍），重跑一次 30000 步产出新 checkpoint。
- **预先定义的判据**（做完对照，不满足就不算通过）：
  - 质心残差 / 每帧位移 **< 0.5**（现在 2.12）
  - v=gt 与 v=0 的 GT 误差差距 **> 3×** 重复跑的标准差（现在是打平）
  - 干净 bank 占比不因幅度变大而下降（**防的就是"信号涨了噪声也涨"**）
- **成本**：小。改合成器 + 生成 + 一次训练。
- **风险**：运动变快可能让跟踪更差、离群更多，于是噪声跟着涨。所以第三条判据是硬性的，**不能只看 PSNR**。
- **实际结果**：三条判据全部达标——残差比 2.12 → **0.41**；v=gt 相对 v=0 改善 **39.9σ**（旧序列是打平）；干净 bank **6/12 → 13/13**（不降反升）。做法不是拉长行程而是 `--motion_cycles 3.5` 多次弹跳，房间/帧数/相机轨迹/扫过体积全部不变。

### 阶段 1：把 4D 参数接进 `GaussianModel`

`gaussian_4d.py` 目前是死代码，只有单测和离线工具用过。

- **做什么**：`time_scale` / `velocity` 作为逐高斯**可训练**参数接线。
- **已查明的契约**（比 §3.28 的 `dynamic_object_id` 难，因为那是 buffer，这两个要进优化器）：
  - `cat_tensors_to_optimizer` 遍历每个 param group 并**要求** `tensors_dict[group["name"]]` 存在——漏一个就是 `KeyError`。所以 `training_setup`、`densification_postfix` 及其三个调用点（`extend_from_pcd`、`densify_and_split`、`densify_and_clone`）、`prune_points` 都要同步。
  - **`save_ply` 不用改**（更正上一版计划）：它用的是固定的标准 3DGS 字段表，新参数不进 ply。ply 是给外部查看器的，保持标准格式；4D 参数只存 checkpoint。
  - `time_scale` **必须** softplus/exp 参数化。原因见 §3.30 审查：裸存时优化器一旦把它推过零，该行永久退化成 δ 且梯度为 0，再也回不来。

#### 设计决策（动手前定死，避免中途改语义）

| 项 | 决定 | 理由 |
|---|---|---|
| `time_scale` 参数化 | 存 raw，`get_time_scale = softplus(raw)`；初值按 `inverse_softplus` 反解 | 保证恒正，杜绝 §3.30 那个"推过零就永久死亡"的陷阱 |
| `velocity` 参数化 | 直接存 3 维 | 无正负约束，不需要变换 |
| 静态行是否也分配参数 | 是，整张量分配 | 与现有所有属性一致；`slice_at_time` 已用 `motion_score` 屏蔽，静态行梯度恒为 0，Adam 在 grad=0 时更新量也是 0，不会漂移 |
| 致密化时的新行 | `densify_and_split/clone` 用 `.repeat(N,1)` 继承父高斯 | 分裂出的子高斯属于同一物体，速度本就该相同（与 `dynamic_object_id` 同款做法） |
| 新 seed 行的初值 | `velocity=0`、`time_scale=` 配置项（快序列上观测间隔中位为 1，取 0.5） | 零速度是无信息先验；半个间隔让相邻 bank 恰好有交叠 |
| checkpoint 版本 | **不升版本**，用 `if "velocity" in tensors` 做字段级兼容，缺失时填默认 | 照 §3.28 加 `dynamic_object_id` 的先例；升版本会让所有旧 checkpoint 被 `load_checkpoint` 直接拒绝 |
| 学习率 | 新增两个配置项，量级对齐 `position_lr`（≈1e-3）；不做衰减调度 | `velocity` 的单位与 xyz 同量级；先不引入第二个调度变量 |

#### 实施步骤（每步都有独立检查点，不要一次性全接上再调试）

1. **只加张量，先不进优化器**（当成 buffer，照 `dynamic_object_id` 走全部传播点）。
   *检查点*：跑一次完整训练，新字段经历致密化/剪枝后行数与语义正确；PSNR 不变（此时还没人读这两个字段，**必须**逐位不变，变了就说明碰坏了别的东西）。
2. **接进优化器**（`training_setup` 加两个 param group，`cat_tensors_to_optimizer`/`_prune_optimizer` 自然覆盖）。
   *检查点*：致密化后优化器 state 的 `exp_avg`/`exp_avg_sq` 行数与参数一致；能 `step()` 不报错；PSNR 仍不变（参数还没接进渲染，梯度恒为 0）。
3. **接进渲染路径**（新 `dynamic_mode = gaussian_4d`，`render_at` 调 `slice_at_time`，eval 路径同步）。
   *检查点*：`time_scale→0` 时与 `oracle_time_slice` 的 bank **逐位相同**（这是 `gaussian_4d.py` 已单测过的性质，此处验证它在真实管线里也成立）。
4. **开训练**。
   *检查点*：`velocity` 非零且分布合理（动态行非零、静态行恒零）；`time_scale` 保持正。
5. **判据评估**（n=5，用 `tools/eval_gaussian_4d_slice.py` 加一路 `v=trained`）。

- **回滚**：每步一个 commit。步骤 1、2 若 PSNR 变了就是碰坏了既有路径，直接回退该步而不是往下调。
- **判据**（基线换成快序列，§3.31 之后旧序列的数值不再适用）：
  - **主判据**：训练出的 velocity 使 GT 误差**显著低于 v=0 的 0.810**，向上界 v=gt 的 0.577 靠拢。该指标 σ≈0.005，n=5 即可判定，分辨率约 40σ——这是阶段 0 换来的能力
  - **守卫判据**：PSNR 不大幅崩坏（快序列同配置基线 15.54，n=1）。**只用它挡崩坏，不用它判定小改善**——外观指标按本项目方法论需 n≥10，而主判据已经足够灵敏
  - 全套测试通过，新增字段经历致密化/剪枝后行数与语义正确（照 §3.28 的检查方式）
- **成本**：中。
- **前置**：**阶段 0 必须先完成**，否则最后一条判据无法判定（现在 v=0 已经打平上界，没有改进空间可言）。

### 阶段 2：动态 bank 的离群治理 — ⚠️ 在快序列上已自行消失，降级为待观察

- **做什么**：先定位再修。离群点特征已知：opacity 正常（0.79~0.96，**不是**僵尸高斯，靠 opacity 过滤没用）、scale 明显偏大、`unique_kfIDs` 显示集中在非 cam0 的 cubemap 面。方向与 §3.11/§3.14 记录的面间质量差异一致。
- **判据**：污染 bank 从 6/12 降到 **≤2/12**；中点里"两侧 knot 都干净"的比例从 2/11 升到 **>80%**。
- **成本**：中，取决于根因落在种子化还是 cubemap 面本身。
- **注意**：稳健质心（中位数/trimmed mean）只是止血——能救 23~26% 污染的 bank，救不了 98% 那种。不要把它当作修复。
- **2026-08-16 更新**：阶段 0 的快序列上污染**自行消失**（13/13 干净），本阶段在该序列上无事可做。但**不能认为根因已解决**：污染消失的机制只是假设（慢速运动使球的位移被误当成视差），未经验证；而"离群集中在非 cam0 的 cubemap 面"这条线索与运动速度无关，真实数据上仍可能复现。保留本阶段，等真实数据或 ERP 光栅化时再判定。

### 阶段 3：换直接 ERP 光栅化（治本，但代价最高）

读 GGPS/PanoLOG 里那份 OmniGS 实现后的判断——**比我上一轮说的要现实一些**：

- **上一轮的说法要修正**：我说过"接口只差一个 `camera_type=3`"。那只在 GGPS **自己那份** rasterizer 里成立。mcgs-slam 的 rasterizer 是另一条派生，两边已分叉：`forward.cu` 差 406 行、`backward.cu` 差 943 行、`auxiliary.h` 差 281 行（mcgs 的 backward 反而更长，1148 vs 931）。**不能整体替换，只能把 LonLat 路径移植过来。**
- **好消息是改动局部**：OmniGS 只替换 preprocess 阶段的投影与二维协方差，tile 分配和 alpha blending 原样复用——而 mcgs 的自有改动集中在 blending/深度/法线侧，两者**正交**。
  - `point3ToLonlatPixel`：`atan2/asin` 球面投影
  - `computeCov2DLonlat`：把 J 换成球面雅可比，`W`、`Vrk`、`T^T Vrk^T T` 与 pinhole 一致
  - `too_close`：剔除 r ≤ 0.2 的点（球面投影在原点奇异）
  - LonLat **不做视锥剔除**（全景处处可见）
- **一个已经做对、移植时务必照搬的点**：LonLat 路径的深度与排序键用的是**径向距离** `p_view.w`，不是 `p_view.z`（pinhole 路径两边都是 `.z`）。SLAM 的跟踪/建图依赖深度语义，用错会静默出错。
- **移植清单**：`auxiliary.h` 3 个 helper（~50 行）、`forward.cu` 的 `computeCov2DLonlat` + `preprocessLonLat`（~120 行）、`backward.cu` 对应反向（~200 行）、`rasterizer_impl.cu` 的免剔除分支、Python 侧 `GaussianRasterizationSettings` 加 `camera_type`。
- **必须先决策**：**OmniGS 是 GPL**（`NOTICE_OMNIGS.md`）。合入 mcgs-slam 会传染，发论文/开源前要先定许可策略。
- **判据**：单次 ERP 渲染的 PSNR ≥ 现在 cubemap 六面的平均；§3.11/§3.14 那类面间差异指标消失；深度输出与 GT 深度的一致性不劣于 cubemap 路径。
- **成本**：高（CUDA + 需要同时改跟踪层的相机模型假设）。

### 阶段 4：真实数据

§3.21 那轮调研之后出现的新候选（§3.30 记录）：360DVO（2026-01，真实 benchmark，Hard 子集含人群动态）、OmniLocalRF 数据集（Insta360 六个户外场景，含行人，MIT）、360Loc、Leader360V。自家 PanoLOG 的 Pano360 拿位姿和管线可能比外部数据容易，但它是**静态**户外。

- **判据**：真 equirect + 相机会动 + 有位姿 GT + 有真实动态物体。四条缺一条就只能做部分实验（§3.21 的四个候选各缺一条）。

---

## 3. 明确不做

- **再调 DeformNet**：六个角度已全部证伪（§3.5–§3.9）
- **从 bank 质心估 velocity/轨迹——但仅限 bank 被污染时**。§3.31 修正：在干净的快序列上 v=est（0.745）反而**优于** v=0（0.810），只是不如 v=gt（0.577）。之前观察到的有害是污染的后果，不是方法本身的性质。仍然优先做可训练版本，但不必把估计版当作禁区
- **在旧的慢速序列上做表示优劣对比**：那里 v=0 与上界打平，结论必然落在噪声里。改用 `data/synth_erp_room_dynamic_fast_hires`
- **用 `--stride` 抽稀来人为拉大时间间隔**：试过，剩下的 bank 大多是污染的，stride 3 只剩 1 个干净行

---

## 4. 顺序与依赖

```
阶段 0（测量能力）── 必须先做
      │
      ├──> 阶段 1（4D 接线 + 训练）   ← 判据依赖阶段 0
      │
      └──> 阶段 2（离群治理）         ← 与阶段 1 可并行
                  │
                  └──> 阶段 3（ERP 光栅化）  ← 阶段 2 的根因若指向 cubemap 面，这一步顺势承接

阶段 4（真实数据）── 与上面全部并行，按调研进度推进
```

**阶段 0 已完成（§3.31）。下一步是阶段 1**：把 `time_scale`/`velocity` 接进 `GaussianModel` 并训练，判据现在是可判定的——目标是训练出的速度把 GT 误差从 v=0 的 0.810 压向上界 v=gt 的 0.577，任何落在这两者之间的改善都能以 40σ 的分辨率被测出来。
