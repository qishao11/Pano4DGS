# MCGS-SLAM 消融实验结果

> **⚠️ 引用本文任何 ATE 数字前必读两条保留**（详见文末对应小节，不要只看表格）：
> 1. **本文所有 ATE 绝对值都建立在一个已确认有 bug 的关键帧选取路径上**（`rm_keyframe` 不维护 `kf_stamps`，而后者参与"距上一关键帧超 3 秒强制建关键帧"的判据）。**相对比较有效，绝对值在修复并重跑前不应对外引用。** → 见"关于本文所有 ATE 绝对值的重要保留"
> 2. 最早那三组 A/B/C 数据是在**只有 2 路建图相机**（不是论文的 3 路）下跑的，已由"修正实验结果"一节的新数据取代。→ 见下方第一节

## ⚠️ 重要更正（2026-08-09）：以下三组数据是在 2 路建图相机下跑出来的，不是论文的三相机协议

核实代码后发现，`--imagedir` 的槽位 1 是 DROID 双目右目占位，图像永不进入建图（`motion_filter.py` 第 114 行 `del indices[1]` 无条件执行），而 `args.multi = len(args.imagedir)`（`options.py:48`）。所以 **N 个目录 ⇒ N-1 路建图相机**。README 要求传 4 个目录（`front front_right front_left front_right`）才能得到论文的三相机配置。

但本次三组实验的产物只有 `pc_1.ply`/`pc_2.ply` 和 `renders/cam0`、`cam1`，反推出 `args.multi=3`，即**只传了 3 个目录、只有 2 路相机参与建图**（front + front_left，front_right 因占据槽位 1 被丢弃）。

**因此下表的 ATE 与论文的差距（+72.5%）很可能主要来自少了一路相机的 rig 约束，而不是算法本身的差异。** 详细核实过程见 `paper_experiment_list.md` 的"槽位约定核实"一节。

## 修正实验结果（2026-08-09，按 README 传 4 个目录 = 3 路建图相机）

GPU 空闲后按 `README` 第 85 行的写法重跑了 A/B/C 三组（`--imagedir front front_right front_left front_right`，其余参数不变），输出在 `output/paper_repro_4cam/`。

**先确认相机数确实变了**（三条独立证据）：运行日志出现 `PhysicalWindow: physical_times=10 active_views=30`（30÷10 = 每帧 3 路，旧跑是 20÷10 = 2 路）；点云从 `pc_1/pc_2` 变成 `pc_1/pc_2/pc_3`；渲染目录从 `cam0/cam1` 变成 `cam0/cam1/cam2`。

| 配置 | PSNR ↑ | SSIM ↑ | LPIPS ↓ | **ATE RMSE (m) ↓** |
|---|---:|---:|---:|---:|
| 论文 | 27.09 | 0.830 | 0.223 | **0.398** |
| A 旧（2 相机） | 27.4784 | 0.8473 | 0.1818 | 0.7231 |
| **A 新（3 相机）** | 27.2691 | 0.8386 | 0.2078 | **0.4262** |
| B 旧（2 相机） | 27.6265 | 0.8528 | 0.1740 | 0.7177 |
| **B 新（3 相机）** | 27.2338 | 0.8385 | 0.2061 | **0.4884** |
| C 旧（2 相机，完整模型） | 27.5051 | 0.8488 | 0.1815 | 0.6864 |
| **C 新（3 相机，完整模型）** | 26.7787 | 0.8210 | 0.2414 | **1.4920** ⚠️ |

**结论一：假设基本证实——补齐第三路相机后 ATE 大幅改善。** A 组 0.7231 → 0.4262（与论文 0.398 的差距从 +81.7% 收窄到 **+7.1%**），B 组 0.7177 → 0.4884（+80.3% → **+22.7%**）。这印证了"少一路相机的 rig 约束"确实是此前 ATE 偏大的主因之一，不是算法实现有问题。

**结论二：外观指标全线小幅下降，且更接近论文值。** 三组 PSNR 都从 ~27.5 降到 ~26.8-27.3、LPIPS 从 ~0.18 升到 ~0.21-0.24。这符合预期：多一路相机就多一组要同时拟合的视角，重建难度上升；而且新数值反而**比旧的 2 相机数值更贴近论文报告值**（论文 27.09/0.830/0.223），进一步说明 3 相机才是论文的真实配置。

**结论三：完整模型 C 的 ATE 反而大幅恶化到 1.4920，与 A/B 的改善方向相反——已重复 3 次确认，是可复现的真实现象，不是噪声。** 在 2 相机时 C 是三组里 ATE 最好的（0.6864），3 相机时却成了最差的，而且 evo 报告的 std 高达 1.0995（A/B 只有 0.14），说明不是整体均匀偏移，而是轨迹中某段出现了局部漂移。

**顺带发现一个对本项目所有后续实验都有用的事实：轨迹/ATE 是确定性的，外观指标不是。** 完整重跑 C 两次（`C_full_rep2`/`C_full_rep3`，与首次跑相隔 18-19 分钟，是货真价实的独立跑）：

| 跑 | ATE RMSE | traj_mcgs.txt md5 | mean PSNR |
|---|---:|---|---:|
| C_full | 1.492029 | `6761020b...` | 26.7787 |
| C_full_rep2 | 1.492029 | `6761020b...` | 26.7061 |
| C_full_rep3 | 1.492029 | `6761020b...` | 26.7610 |

三次的 `traj_mcgs.txt` **逐字节完全相同**（md5 一致），而 PSNR 三次都不同——后者证明这确实是三次独立的跑，不是同一份结果被读了三遍。这跟 `panoramic_4dgs_status.md` §3.13 的结论吻合：`traj_mcgs.txt` 由 `Mcgs.save_kf_poses()` 从 `video.globuf` 导出，走的是 DROID 跟踪自己的状态，跟非确定性的 GS 精修阶段完全无关。

**方法论推论（补充 §3.24 的教训）**：本项目里 **ATE 类指标 n=1 就够**（确定性），**外观指标（PSNR/SSIM/LPIPS）需要 n≥10**（噪声标准差接近效应量级）。上表 A/B 的 ATE 虽然只跑了一次，但按此结论是可信的，不需要重复。

**按论文的 ATE 取优规则**（prior depth 下 JDSA 开/关取较小者）：min(B=0.4884, C=1.4920) = **0.4884 m**，相对论文 0.398 m 高 22.7%——比修正前的 0.6864 m（+72.5%）改善明显，但仍有差距。

## JDSA 劣化的排查：找到一个真实的索引错配 bug，但"修好"它反而让结果差 5 倍——假设被推翻，改动已撤销（2026-08-10）

**第一步，发现了一处真实的变量泄漏**。`depth_video.py::multi_cam_ba()` 里：

```python
for ii, jj, T_ci_c0 in zip(iis, jjs, self.T_ci_c0):   # 第 410 行，循环变量泄漏到函数作用域
    ...
if self.use_jdsa and use_scaling:
    for i, disps in enumerate(self.disps_list):
        JDSA(targets[i], weights[i], etas[i], ..., ii, jj, ...)   # 第 433 行，用的是泄漏的 ii/jj
```

Python 的 for 循环变量没有块作用域，所以第 410 行结束后 `ii/jj` 保留着**最后一路相机**的边索引，第 433 行给每一路相机调用 JDSA 时 `targets/weights/etas` 按相机正确取、但 `ii/jj` 恒为最后一路的。每路相机有各自独立的因子图（`droid_frontend.__graph_update()` 逐图收集 `iis/jjs`），所以这些数组确实不同。

**加临时诊断实测确认（Waymo 100613，3 相机）**：

```
cam=0  leaked_ii.shape=(48,)  own_ii.shape=(58,)  identical=False   ← 错配
cam=1  leaked_ii.shape=(48,)  own_ii.shape=(48,)  identical=True
cam=2  leaked_ii.shape=(48,)  own_ii.shape=(48,)  identical=True
```

受影响的恰好是 **cam0**——主跟踪相机，其视差直接决定导出的轨迹。它自己 58 条边的对应数据被配上了 48 条边的索引，**长度都不一致**。所以"索引错配"这件事本身是**已证实的真实现象**，不是猜测。

**第二步，按"正确"配对改成 `iis[i], jjs[i]` 后重跑——结果反而大幅恶化，假设被推翻：**

| 配置（均为 3 相机） | ATE RMSE (m) | PSNR |
|---|---:|---:|
| C 完整模型，原代码（错配的 ii/jj） | 1.4920 | 26.7787 |
| **C 完整模型，改用 `iis[i]/jjs[i]`** | **7.8672** ⚠️ | 24.3177 |
| B（不开 JDSA，作为健全性检查，改动应无影响） | 0.488448 | 27.2971 |

ATE 从 1.49 恶化到 **7.87，差了 5 倍多**，外观指标也同步下降（26.78 → 24.32）。**健全性检查通过**：B 组（不开 JDSA）的 ATE 是 0.488448，与改动前那次 B 跑**完全一致到小数点后 6 位**，证明这个改动确实只影响 JDSA 路径、没有意外副作用，同时再次印证了 ATE 的确定性。

**结论：索引错配是真的，但它不是 ATE 劣化的原因——"修正"配对反而让情况严重得多，原来的假设被实验推翻。改动已在本地和 ashton 两侧完整撤销**，代码回到未改动状态（即上面表格里 1.4920 那一行对应的状态）。

**第三步，先读懂 JDSA 的输入约定，再针对性地做隔离实验。** 读 `geom/ba.py::JDSA()` 确认了约定：`droid_backends.proj_trans(poses, disps, intrinsics, target, weight, ii, jj)` 要求 `target/weight` 与 `ii/jj` **逐元素一一对应**，随后对 `unique(ii)` 覆盖的帧求解每帧的 2×2 深度尺度场（`dscales`），并写回这些帧。也核实了辅助相机的先验深度确实被正确填充（`item[5][ic]` 逐相机对应，不是空的）——所以"辅助相机没有先验深度"这个可能性也排除了。

据此做了一组隔离实验，用环境变量切换变体（默认路径不变）：

| 变体（均为 3 相机 + `--prgbd --jdsa`） | ATE RMSE (m) | PSNR |
|---|---:|---:|
| **不开 JDSA（对照基准）** | **0.4884** | 27.2971 |
| 原代码（错配索引，全部相机） | 1.4920 | 26.7787 |
| **原代码复核（env-var 脚手架下重跑，对照组）** | **1.492029** ✓ | 26.8119 |
| 只对 cam0 应用 JDSA，用正确索引 | 5.6834 | 25.1056 |
| 全部相机，用正确索引 | 7.8672 | 24.3177 |

**对照组有效**：加了 env-var 脚手架后走原路径重跑，ATE 复现到 **1.492029，与之前完全一致到小数点后 6 位**，证明脚手架本身没有改变默认行为，上表所有对比都是有效的。

**结果呈现出一个清晰且反直觉的规律：JDSA 被"越正确地"应用，ATE 就越差**（0.4884 关闭 → 1.4920 错配 → 5.6834 只修 cam0 → 7.8672 全部修正）。也就是说，**那个索引错配 bug 恰恰在无意中削弱了 JDSA 的作用，反而起到了保护效果**；"只有辅助相机的 JDSA 有问题"这个假设也被否定了——只对主相机 cam0 应用正确的 JDSA，ATE 依然从 0.4884 恶化到 5.6834。

**最终结论：问题不在索引配对，而在于 JDSA 的深度尺度联合优化在这份数据的多相机场景下本身就是有害的。** 不存在"改一行索引"式的修复；把索引改"对"只会让有害的优化跑得更彻底。**代码已完整撤销回原状**（本地 + ashton 均已确认干净），保留原有的错配写法——不是因为它正确，而是因为在没搞清楚 JDSA 为何发散之前，任何改动都只会让结果更差，且当前配置下这个路径本来就不该启用。

**给论文复现的实际建议**：本机 Waymo 100613 的 3 相机配置下应当**关闭 JDSA**（ATE 0.4884），这也正是论文"取 JDSA 开/关较优者"规则会自动选中的配置，所以复现结论不受影响。

## 第四步：追踪 `dscales` 演化，定位到两个独立的致害机制（2026-08-10 续）

按上一节留下的方向，给 JDSA 调用点加了 env-var 隔离的追踪（记录每次调用前后的 `dscales`/`disps` 极值与先验覆盖率），跑 `--early_stop 40`，共 195 次 JDSA 调用。

**机制一：`dscales` 单调塌缩（规范自由度漂移）。** `dscales` 初值为 1.0（`torch.ones`），是乘在先验深度上的每帧 2×2 尺度场。实测它单调下滑且从不回升：

| | 初始 | call 16 | call 32 | call 48 | call 64 |
|---|---|---|---|---|---|
| cam0 mean | 0.977 | 0.712 | 0.687 | 0.688 | 0.698 |
| cam1 mean | 0.949 | 0.603 | 0.489 | 0.441 | **0.409 ↓仍在降** |
| cam2 mean | 0.982 | 0.596 | 0.506 | 0.440 | **0.407 ↓仍在降** |

**为什么这会毁掉 ATE**：`evo_ape -as` 做的是 Sim(3) 对齐（含全局尺度校正），**恒定的全局尺度误差本会被吸收**。ATE 依然差，说明尺度误差是**沿轨迹渐变的**（早期帧 ≈0.98、后期 ≈0.41），这种平滑增长的尺度漂移单次 Sim(3) 对齐救不回来。**机理**：重投影残差对"深度尺度 + 平移尺度"的联合缩放不变，`dscales` 因此是欠约束的规范自由度，而 JDSA 的求解里既没有拉回 1.0 的先验项、也没有锚定帧，所以它会自己漂走。这也解释了上一节那个反直觉现象——索引改得越"正确"，JDSA 更新越有效，塌缩越快，ATE 越差。

**机制二：JDSA 的视差更新本身破坏几何。** 追踪显示 cam0 的 `disp_max` 在连续几次调用间从 0.15 → 1.31 → 6.99 → 8.93 一路炸开（视差 8.9 对应深度约 0.11 m，物理上不可能；作为对照，cam2 全程稳定在 0.2 左右）。JDSA 除了更新尺度，还通过 `disp_retr()` 直接改写视差图，这条通路独立于尺度。

**用夹紧实验把两个机制分开：**

| 变体（3 相机 + `--prgbd --jdsa`） | ATE RMSE (m) | PSNR |
|---|---:|---:|
| **JDSA 关闭（基准）** | **0.4884** | 27.2971 |
| JDSA 开，`dscales` 自由（原代码） | 1.4920 | 26.7787 |
| JDSA 开，`dscales` 夹紧到 [0.9, 1.1] | 1.1262 | 26.7766 |
| JDSA 开，`dscales` 冻结到 [0.99, 1.01] | **1.0508** | 26.5951 |

**结论：两个机制都真实存在，且以第二个为主。** 尺度控制得越紧，ATE 单调改善（1.4920 → 1.1262 → 1.0508），**证实机制一确实是真实的致害因素，约占总损害的 30%**（1.4920 → 1.0508）。但即便把尺度完全冻结在 1.0 附近，ATE 仍是 1.0508，是 JDSA 关闭时（0.4884）的 **2.15 倍**——**剩下约 70% 的损害来自机制二（视差更新），与尺度漂移无关**。

**最终定论：多相机 JDSA 在这份数据上有两条独立的致害通路，都不是索引问题，也都不是加个夹紧就能修好的。** "给 `dscales` 加锚定/先验"只能拿回约三成，真正的大头在视差更新通路，要修需要重新审视 JDSA 的视差残差与 BA 的关系（为何两者叠加会把视差推到物理上不可能的量级），属于算法层面的重新设计，不是打补丁。**所有诊断代码已完整撤销**（`depth_video.py` 与 `geom/ba.py` 本地和 ashton 均已确认回到干净状态）。在做这项重新设计之前，本机复现应当保持 JDSA 关闭。

**对论文复现的实际影响**：无论用哪种索引配对，3 相机下开 JDSA 的 ATE（1.49 或 7.87）都远差于关掉 JDSA（0.4884）。所以按论文"取 JDSA 开/关较优者"的规则，本机结果仍取 **0.4884 m** 不变。

## ⚠️ 关于本文所有 ATE 绝对值的重要保留（2026-08-10 代码审查发现）

代码审查发现并**实测确认**了一个真实的不变量违反：`depth_video.py::rm_keyframe()` 移除关键帧时把 `tstamp`/`poses`/`images`/`disps` 等所有缓冲区都往下移一格，**唯独漏了 `kf_stamps`**（同文件的 `release_buffer()` 是维护它的，说明这个不变量是有意设计的）。加探针实测（Waymo 100613）：**每一次 `rm_keyframe` 调用都 desync，且调用非常频繁**（前 40 帧内连续触发十余次），错位一个关键帧并累积。

`kf_stamps` 的下游消费者有两条：`mcgs.py::_source_time_tensor` →`physical_tstamp`（DeformNet 的时间输入 / oracle 时间切片绑定），以及 `global_buffer.fill_global_data` →`kf_stamps_all` →**`traj_mcgs.txt` 的时间戳 → `evo_ape` 用它把位姿与 GT 配对**。

**尝试修复（补上 `self.kf_stamps[ix] = self.kf_stamps[ix+1]`）后重跑，时间戳确实变正确了，但 ATE 反而变差：**

| 配置 | 修复前 ATE | 修复后 ATE | 末尾时间戳（GT 实际末尾 …171.823） |
|---|---:|---:|---|
| A | 0.4262 | **0.5446** | …170.123 → **…171.323** |
| B | 0.4884 | **0.6796** | …170.123 → **…171.323** |

时间戳侧修复是对的（修复后覆盖了真实采集跨度；修复前整体前移、比序列结尾早 1.7 秒），PSNR 基本不变（27.27→27.34、27.23→27.13）。

**追查 ATE 为何变差——我最初的判断（"位姿侧还有第二个错位在相互抵消"）已被实验推翻，真实原因是这个 bug 会改变关键帧选取本身。**

第一步先查了怀疑的位姿侧配对错位：`save_kf_poses()` 用 `sorted(kf_stamps_all.values())` 与 `poses_all` **按位置 zip**，看着很脆弱。加探针实测后**排除**：`N=total_counter=47`、`len(kf_stamps_all)=47`、`len(poses_all)=47`、键 `[0..46]` 连续无空洞、zip 无截断——配对是 1:1 正确的。

第二步找到真正原因：**`kf_stamps` 不只是标签，它还参与关键帧选取判据**。`motion_filter.py:147`：

```python
if delta.norm(...).mean().item() > self.thresh or (tstamp - self.video.kf_stamps[self.video.counter.value-1]) > 3:
```

即"距上一关键帧超过 3 秒则强制新建关键帧"。此前（包括代码审查的意见）都认为这个读取是安全的，理由是 `kf_stamps[counter-1]` 每次 append 时刚写过——**但 `rm_keyframe` 递减 counter 之后，它指向的正是被污染的那个槽位**。

**实测验证：修复前后两条轨迹的关键帧集合只有 19.1% 重合**（各 47 帧，其中 38 帧不同）。

**所以修复前后不是"同一条轨迹换了标签"，而是两次选取了几乎完全不同关键帧的、不同的跟踪运行。** ATE 从 0.4262 变成 0.5446，并不能推出"存在第二个补偿性 bug"，只能说明在这一条序列上、修正后的关键帧选取恰好得到了更差的轨迹——而每种配置都只有 n=1。

**处理方式：修复已撤销**（本地与 ashton 均确认干净），代码保持原状，以便本文所有数字仍在同一口径下可比。

**对本文结论的影响，如实界定：**
- **相对比较仍然有效**：本文所有对比（2 相机 vs 3 相机、A/B/C 之间、JDSA 开/关、各夹紧变体）都是在同一套口径下跑的，相互之间可比，"补齐第三路相机使 ATE 大幅改善"这一核心结论不受影响。
- **ATE 绝对值建立在一个已确认有 bug 的关键帧选取逻辑之上**，与论文数字（0.398）的差距百分比（+7.1% / +22.7%）在该 bug 修好并重跑之前，不应作为最终结论对外引用。
- **下一步**：这个修复本身是正确的（时间戳语义、关键帧选取判据两方面都应该用真实的上一关键帧时间），但因为它会改变关键帧选取，**不能靠单条序列的 ATE 来判断它是好是坏**——需要在多条序列上评估，而本机目前只有 `100613` 一条可用（其余序列缺原始多相机数据，见 `paper_experiment_list.md`）。在拿到更多序列之前，这个修复保持未应用状态并记录在案。

## 代码完整性

- 未修改任何论文源码。
- 三组实验使用相同的 `demo.py`、`config/config.yaml`、权重、三相机标定、198 帧输入和 30,000 步离线全局精修。
- 变量仅为论文公开的命令行开关 `--prgbd` 和 `--jdsa`。
- 使用场景：Waymo `100613`，47 个关键帧。

## 三组配置

| 组别 | 命令行开关 | 含义 |
|---|---|---|
| A | 无 | w/o Depth + w/o JDSA |
| B | `--prgbd` | w/ Depth + w/o JDSA |
| C | `--prgbd --jdsa` | MCGS-SLAM full |

## 本机 100613 结果

| 配置 | PSNR ↑ | SSIM ↑ | LPIPS ↓ | ATE RMSE (m) ↓ |
|---|---:|---:|---:|---:|
| A：w/o Depth + w/o JDSA | 27.4784 | 0.8473 | 0.1818 | 0.7231 |
| B：w/ Depth + w/o JDSA | **27.6265** | **0.8528** | **0.1740** | 0.7177 |
| C：MCGS-SLAM full | 27.5051 | 0.8488 | 0.1815 | **0.6864** |

## 论文 Table V（六序列平均）

论文 Table V 使用 `134763`、`106762`、`132384`、`152706`、`153495`、`163453` 六个 Waymo 序列的平均值，不包含 `100613`。

| 配置 | PSNR ↑ | SSIM ↑ | LPIPS ↓ |
|---|---:|---:|---:|
| A：w/o Depth + w/o JDSA | 25.01 | 0.751 | 0.404 |
| B：w/ Depth + w/o JDSA | 27.02 | 0.809 | 0.271 |
| C：MCGS-SLAM full | 27.17 | 0.816 | 0.262 |

## 观察

- 在 `100613` 上，加入 prior depth（A → B）带来小幅外观改善：
  - PSNR `+0.1481 dB`
  - SSIM `+0.0055`
  - LPIPS `-0.0078`
- 在 `100613` 上，加入 JDSA（B → C）没有继续改善外观，但 ATE 从 `0.7177 m` 降至 `0.6864 m`。
- 单个 `100613` 的趋势与论文六序列平均趋势不完全一致；不能据此否定或复现 Table V 的总体结论。
- 严格复现 Table V 仍需要上述六个序列的三相机原始 RGB、GT 和逐序列标定。

## 输出目录

- A：`output/paper_ablation/waymo_100613_no_depth_no_jdsa`
- B：`output/paper_repro/waymo_100613_jdsa_off`
- C：`output/paper_repro/waymo_100613_jdsa_on`
